{
  "name": "specular",
  "version": "0.0.1",
  "description": "Build lightshows with React and control them with DMX",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --stage 0 --out-dir lib/ src/",
    "watch": "babel --watch --stage 0 --out-dir lib/ src/",
    "prepublish": "npm run build"
  },
  "author": "Jamie Greeff <jamiegreeff+git@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "artnet": "0.0.3",
    "invariant": "^2.1.0",
    "lodash": "^3.9.3",
    "midi": "^0.9.3",
    "socket.io": "^1.3.5",
    "tween-functions": "^1.0.2"
  },
  "peerDependencies": {
    "react": "^0.14.0-beta3"
  },
  "devDependencies": {
    "babel": "^5.8.19"
  }
}
